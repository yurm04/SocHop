sochopApp.direvtive('googleMaps', function ($window, $parse) {
  return {
    restrict : 'A',
    replace : false,
    templateUrl : '/src/partials/map.html',
    link : function (scope, element, attrs, controller) {
      // map directive
    }
  };
});